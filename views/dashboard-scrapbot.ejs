<%
  // Render this page using the shared dashboard layout shell.
  // Keeps the Scrapbot area visually consistent with the rest of the dashboard.
  tabView = 'tabs/scrapbot';
%>
<%- include('layout') %>
