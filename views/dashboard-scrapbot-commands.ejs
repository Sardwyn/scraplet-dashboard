<%
  // Render this page using the shared dashboard layout shell.
  // IMPORTANT: use the commands tab, not the scrapbot landing tab.
  tabView = 'tabs/scrapbot-commands';
%>
<%- include('layout') %>
